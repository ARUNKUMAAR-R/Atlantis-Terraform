version: 3
projects:
  - name: azure-network
    dir: network
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
    extra_args:
      plan:
        - "-var-file=vars_dev.tfvars"
      apply:
        - "-var-file=vars_dev.tfvars"

